config:
  MD013: false
  MD024:
    siblings_only: true
  MD025: false

ignores:
  - .git/**
  # Absent in CI, so ignoring it locally is what makes the two runs agree.
  - node_modules/**
  - docs/Repository_Template_Development_Guide.md
